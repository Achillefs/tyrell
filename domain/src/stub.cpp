// Placeholder TU so vp330_domain has something to compile until Phase 1 lands
// real value types and entities. Delete when the first real .cpp arrives.
namespace vp330::detail {
/**
 * @brief Provides a no-op anchor to make the translation unit non-empty.
 *
 * This function intentionally has an empty body and exists only as a placeholder
 * until real implementations/types are added.
 */
inline void anchor() {
}
} // namespace vp330::detail
